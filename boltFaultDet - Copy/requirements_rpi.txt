# Raspberry Pi optimized requirements (ARM64/ARMv7)

# Web Framework
flask==3.0.0

# Computer Vision and Image Processing (Raspberry Pi optimized)
opencv-python-headless==4.8.1.78  # Headless version for better Pi performance
pillow==10.1.0
numpy==1.24.4

# AI/ML Framework (Raspberry Pi compatible versions)
ultralytics==8.0.206
torch==2.1.0+cpu  # CPU-only version for Pi
torchvision==0.16.0+cpu  # CPU-only version for Pi

# Hardware Communication
pyserial==3.5

# Additional Pi-specific optimizations
psutil==5.9.6  # System monitoring
RPi.GPIO==0.7.1  # GPIO control (if needed)

# Performance optimization libraries
scipy==1.11.4  # Scientific computing
requests==2.31.0  # HTTP requests

# Installation notes:
# For Raspberry Pi, install PyTorch with:
# pip3 install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# 
# For better performance on Pi 4, consider:
# sudo apt-get install libatlas-base-dev libopenblas-dev
# 
# Memory optimization for Pi:
# export PYTORCH_DISABLE_CUDA_MEMORY_CACHING=1